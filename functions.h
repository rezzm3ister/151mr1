#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <iostream>
#include <vector>
#include <fstream>
#include <sstream>
#include "functions.h"

using namespace std;

bool datain(vector<double> &data, char dtype, int &start);

bool isDouble(string s, double& value);


#endif