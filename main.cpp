#include <iostream>
#include <vector>
#include <fstream>
#include <sstream>
#include "functions.h"
using namespace std;

int main()
{
  vector<double> xdata, ydata;
  int xstart, ystart;
  char dtype='x';
  /*
  while(!datain(xdata, dtype, xstart))
  {
  }

  dtype='y';
  while(!datain(ydata,dtype,ystart))
  {
  }
  */
 datain(xdata,dtype,xstart);


  
  
}